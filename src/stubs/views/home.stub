@extends('layouts.app')

@section('content')
    <h1>{{ __('Dashboard') }}</h1>

    <div>
        @if (session('status'))
            <div>
                {{ session('status') }}
            </div>
        @endif

        {{ __('You are logged in!') }}
    </div>
@endsection
